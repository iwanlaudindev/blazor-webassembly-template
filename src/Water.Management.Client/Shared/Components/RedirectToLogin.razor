@inject NavigationManager _navigationManager

@code {
    protected override void OnInitialized()
    {
        _navigationManager.NavigateTo($"/auth/signin?returnUrl={Uri.EscapeDataString(_navigationManager.Uri)}", true);
    }
}
